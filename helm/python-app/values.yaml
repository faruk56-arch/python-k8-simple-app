# Default values for python-app.
replicaCount: 3

image:
  repository: faruk67/my-python-k8s-app
  # Overrides the image tag whose default is the chart appVersion.
  tag: ab715625d9c2a3f7f942ae3a67907f245ae60063
  pullPolicy: Always


service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: "python-app.local"
      paths:
        - path: /
          pathType: Prefix
    - host: ""
      paths:
        - path: /
          pathType: Prefix


resources: 
  requests:
    cpu: 250m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 128Mi

env:
  ENVIRONMENT: DEVELOPMENT

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50